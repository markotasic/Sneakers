*Delete item image(s) from the folder when item is edited and changed
*If item has some images in the mongo, and there is a new one added, add that image to an array without deleting the image
*Change the name of the images for the ItemData.name 